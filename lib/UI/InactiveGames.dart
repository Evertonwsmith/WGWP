import 'package:flutter/material.dart';

class InactiveGames extends StatelessWidget {
  const InactiveGames({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
